<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:t="clr-namespace:Strunika.Mobile.Theme"
             xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
             xmlns:loc="clr-namespace:Strunika.Mobile.Localization"
             xmlns:controls="clr-namespace:Strunika.Mobile.Controls"
             xmlns:vm="clr-namespace:Strunika.Mobile.ViewModels"
             x:Class="Strunika.Mobile.Pages.AddSongSheet"
             x:DataType="vm:LibraryViewModel"
             ios:Page.UseSafeArea="True"
             ios:Page.ModalPresentationStyle="FormSheet"
             NavigationPage.HasNavigationBar="False">

    <Grid BackgroundColor="{t:Theme Bg}" Padding="20,16,20,28" RowDefinitions="Auto,Auto,*,Auto">
        <Grid Grid.Row="0" ColumnDefinitions="*,44">
            <Label Style="{StaticResource LargeTitle}" FontSize="28" Text="{loc:Str Library_Add}" VerticalOptions="Center" />
            <Border Grid.Column="1" WidthRequest="36" HeightRequest="36" StrokeShape="RoundRectangle 18" BackgroundColor="{t:Theme Surface2}" Padding="0" HorizontalOptions="End">
                <controls:IconView Name="x" Size="18" Color="{t:Theme TextPri}" HorizontalOptions="Center" VerticalOptions="Center" />
                <Border.GestureRecognizers><TapGestureRecognizer Tapped="OnCloseTapped" /></Border.GestureRecognizers>
            </Border>
        </Grid>
        <Label Grid.Row="1" x:Name="QuotaLabel" Style="{StaticResource Footnote}" Margin="0,6,0,0" IsVisible="False" />

        <VerticalStackLayout Grid.Row="2" Spacing="10" Margin="0,16,0,0">

            <!-- YouTube -->
            <Border Style="{StaticResource Card}" Padding="14,12">
                <Grid ColumnDefinitions="44,*,40" ColumnSpacing="12">
                    <Border WidthRequest="44" HeightRequest="44" StrokeShape="RoundRectangle 12" BackgroundColor="{t:Theme Surface2}" StrokeThickness="0" Padding="0">
                        <controls:IconView Name="youtube" Size="22" Color="{t:Theme AccentText}" HorizontalOptions="Center" VerticalOptions="Center" />
                    </Border>
                    <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                        <Label Text="{loc:Str Library_YouTube}" FontSize="17" FontAttributes="Bold" TextColor="{t:Theme TextPri}" />
                        <Label Text="{loc:Str Library_YouTube_Hint}" Style="{StaticResource Footnote}" />
                    </VerticalStackLayout>
                    <Border Grid.Column="2" WidthRequest="40" HeightRequest="40" StrokeShape="RoundRectangle 20" BackgroundColor="{t:Theme Surface1}" StrokeThickness="0" Padding="0" VerticalOptions="Center">
                        <controls:IconView Name="star" Size="20" Color="{t:Theme Dim}" HorizontalOptions="Center" VerticalOptions="Center">
                            <controls:IconView.Triggers>
                                <DataTrigger TargetType="controls:IconView" Binding="{Binding PinYouTube}" Value="True">
                                    <Setter Property="Color" Value="{t:Theme AccentText}" />
                                    <Setter Property="StrokeSize" Value="2.6" />
                                </DataTrigger>
                            </controls:IconView.Triggers>
                        </controls:IconView>
                        <Border.GestureRecognizers><TapGestureRecognizer Command="{Binding TogglePinCommand}" CommandParameter="youtube" /></Border.GestureRecognizers>
                    </Border>
                </Grid>
                <Border.GestureRecognizers><TapGestureRecognizer Tapped="OnYouTubeTapped" /></Border.GestureRecognizers>
            </Border>

            <!-- File -->
            <Border Style="{StaticResource Card}" Padding="14,12">
                <Grid ColumnDefinitions="44,*,40" ColumnSpacing="12">
                    <Border WidthRequest="44" HeightRequest="44" StrokeShape="RoundRectangle 12" BackgroundColor="{t:Theme Surface2}" StrokeThickness="0" Padding="0">
                        <controls:IconView Name="file" Size="22" Color="{t:Theme AccentText}" HorizontalOptions="Center" VerticalOptions="Center" />
                    </Border>
                    <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                        <Label Text="{loc:Str Library_FromFile}" FontSize="17" FontAttributes="Bold" TextColor="{t:Theme TextPri}" />
                        <Label Text="{loc:Str Library_FromFile_Hint}" Style="{StaticResource Footnote}" />
                    </VerticalStackLayout>
                    <Border Grid.Column="2" WidthRequest="40" HeightRequest="40" StrokeShape="RoundRectangle 20" BackgroundColor="{t:Theme Surface1}" StrokeThickness="0" Padding="0" VerticalOptions="Center">
                        <controls:IconView Name="star" Size="20" Color="{t:Theme Dim}" HorizontalOptions="Center" VerticalOptions="Center">
                            <controls:IconView.Triggers>
                                <DataTrigger TargetType="controls:IconView" Binding="{Binding PinFile}" Value="True">
                                    <Setter Property="Color" Value="{t:Theme AccentText}" />
                                    <Setter Property="StrokeSize" Value="2.6" />
                                </DataTrigger>
                            </controls:IconView.Triggers>
                        </controls:IconView>
                        <Border.GestureRecognizers><TapGestureRecognizer Command="{Binding TogglePinCommand}" CommandParameter="file" /></Border.GestureRecognizers>
                    </Border>
                </Grid>
                <Border.GestureRecognizers><TapGestureRecognizer Tapped="OnFileTapped" /></Border.GestureRecognizers>
            </Border>

            <!-- Record -->
            <Border Style="{StaticResource Card}" Padding="14,12">
                <Grid ColumnDefinitions="44,*,40" ColumnSpacing="12">
                    <Border WidthRequest="44" HeightRequest="44" StrokeShape="RoundRectangle 12" BackgroundColor="{t:Theme Surface2}" StrokeThickness="0" Padding="0">
                        <controls:IconView Name="mic" Size="22" Color="{t:Theme AccentText}" HorizontalOptions="Center" VerticalOptions="Center" />
                    </Border>
                    <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                        <Label Text="{loc:Str Library_Record}" FontSize="17" FontAttributes="Bold" TextColor="{t:Theme TextPri}" />
                        <Label Text="{loc:Str Library_Record_Hint}" Style="{StaticResource Footnote}" />
                    </VerticalStackLayout>
                    <Border Grid.Column="2" WidthRequest="40" HeightRequest="40" StrokeShape="RoundRectangle 20" BackgroundColor="{t:Theme Surface1}" StrokeThickness="0" Padding="0" VerticalOptions="Center">
                        <controls:IconView Name="star" Size="20" Color="{t:Theme Dim}" HorizontalOptions="Center" VerticalOptions="Center">
                            <controls:IconView.Triggers>
                                <DataTrigger TargetType="controls:IconView" Binding="{Binding PinRecord}" Value="True">
                                    <Setter Property="Color" Value="{t:Theme AccentText}" />
                                    <Setter Property="StrokeSize" Value="2.6" />
                                </DataTrigger>
                            </controls:IconView.Triggers>
                        </controls:IconView>
                        <Border.GestureRecognizers><TapGestureRecognizer Command="{Binding TogglePinCommand}" CommandParameter="record" /></Border.GestureRecognizers>
                    </Border>
                </Grid>
                <Border.GestureRecognizers><TapGestureRecognizer Tapped="OnRecordTapped" /></Border.GestureRecognizers>
            </Border>
        </VerticalStackLayout>

        <Label Grid.Row="3" Text="{loc:Str Library_Pin_Hint}" Style="{StaticResource Caption}" HorizontalTextAlignment="Center" Margin="0,16,0,0" />
    </Grid>
</ContentPage>
